const { execSync } = require('child_process');

module.exports = async () => {
  // Assuming seeder.ts is located at the root of the project
  // execSync('ts-node src/database/seeders/seeder.ts', { stdio: 'inherit' });
};
