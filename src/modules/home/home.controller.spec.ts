describe('HomeController', () => {
  beforeEach(() => {});

  describe('getHomeProfiles', () => {
    it('test normal case [return profiles array]', async () => {
      // random 반환이고, mentorProfileService 는 검증된 상태, homeService 는 아주 단순한 형태이기에 검증 생략
    });
  });

  describe('getHomeProfilesByCategory', () => {
    it('test normal case [return profiles array]', async () => {
      // random 반환이고, mentorProfileService 는 검증된 상태, homeService 는 아주 단순한 형태이기에 검증 생략
    });
  });
});
