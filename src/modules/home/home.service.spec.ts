describe('HomeService', () => {
  describe('random', () => {
    it('test normal case [return random array]', () => {
      // random 함수는 Math.random() 을 사용하기 때문에, 테스트가 불가능하다.
    });
  });

  describe('getRecentUpdatedMentorSortQuery', () => {
    it('test normal case', () => {
      // 아주 단순한 경우이기에 생략함.
    });
  });

  describe('getHomeProfileSortQuery', () => {
    it('test normal case', () => {
      // 아주 단순한 경우이기에 생략함.
    });
  });
});
